## Summary

<!-- What does this PR change, and why? -->

## Actor model

- [ ] New behavior uses actors / messages rather than shared mutable workflow state (or explains why not)

## Tests

- [ ] Unit tests updated (`AgctorSDK.Core.Tests`)
- [ ] Integration tests updated when the change crosses process or HTTP/MCP boundaries
- [ ] `dotnet build Agctor.sln` and the relevant `dotnet test` projects pass locally

## Docs and attribution

- [ ] README / XML comments updated if a public API changed
- [ ] CHANGELOG.md Unreleased section updated for user-visible changes
- [ ] No secrets, local config, or build artifacts included
